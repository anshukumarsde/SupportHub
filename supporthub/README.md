# SupportHub app

This folder contains the SupportHub Next.js application.

```powershell
npm install
npm run dev
```

The dashboard is in `app/page.tsx`, and the ticket list is in
`app/TicketTable.tsx`.
