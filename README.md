# SupportHub

SupportHub is a beginner-friendly customer support app. A support team can
use it to look at customer questions and see whether each question is still
open, being handled, or finished.

This is the first version of the app. It uses a local SQLite database, so new
tickets are saved on the computer running the app.

## Technologies used

- **Next.js** - the framework that runs the web application
- **React** - used to build the page and reusable components
- **TypeScript** - adds type checking to the JavaScript code
- **Tailwind CSS** - used to style the dashboard
- **PostCSS** - helps process the Tailwind CSS styles
- **ESLint** - checks the code for common mistakes and style problems
- **SQLite** - stores tickets in a local database file
- **better-sqlite3** - lets the Next.js server read and write SQLite
- **npm** - installs the libraries and runs the project commands

## How the app works

When you open SupportHub:

1. Next.js loads the page in `supporthub/app/page.tsx`.
2. The page reads tickets from the local SQLite database.
3. The page sends that list to `TicketTable`.
4. `TicketTable` displays each ticket in a table.
5. The new-ticket form sends new tickets to the tickets API.
6. The API saves the ticket in SQLite, and the page refreshes to show it.
7. You can change a ticket status directly in the table.
8. Tailwind CSS provides the colors, spacing, borders, and layout.

In simple terms, `page.tsx` decides **what information the page has**, and
`TicketTable.tsx` decides **how the ticket list looks**.

## What you can see

- The total number of tickets
- The number of open tickets
- The number of tickets being worked on
- The number of done tickets
- Recent customer questions
- The person who asked each question
- The current status of each question
- A way to change one ticket to a new status

## Project folders and files

The actual Next.js project is inside the `supporthub` folder:

```text
SupportHub/
├── README.md
└── supporthub/
    ├── app/
    ├── package.json
    ├── package-lock.json
    ├── tsconfig.json
    └── ...
```

### Files in `supporthub/app`

| File | What it does |
| --- | --- |
| `page.tsx` | Builds the main dashboard, reads tickets from the database, and shows the summary numbers. |
| `TicketTable.tsx` | A reusable React component that displays tickets in rows and columns. |
| `NewTicketForm.tsx` | Shows the form for creating a new ticket. |
| `api/tickets/route.ts` | Provides the API for reading tickets, creating tickets, and changing status. |
| `layout.tsx` | Provides the shared page wrapper, browser title, fonts, and global CSS connection. |
| `globals.css` | Loads Tailwind CSS and contains styles that apply across the app. |
| `favicon.ico` | The small icon shown in the browser tab. |

### Configuration files

| File | What it does |
| --- | --- |
| `package.json` | Lists the libraries the app uses and the commands available through npm. |
| `package-lock.json` | Records the exact versions of installed libraries so installs are consistent. |
| `tsconfig.json` | Tells TypeScript how to check the project code. |
| `next.config.ts` | The place for Next.js settings. It is mostly empty because the app does not need custom settings yet. |
| `postcss.config.mjs` | Connects Tailwind CSS to the build process. |
| `eslint.config.mjs` | Contains rules used to find common code problems. |
| `next-env.d.ts` | Generated TypeScript information used by Next.js. Do not edit this file manually. |
| `.gitignore` | Keeps generated folders and private files out of Git. |

### Database files

| File | What it does |
| --- | --- |
| `lib/db.ts` | Creates the SQLite table and provides functions for reading and saving tickets. |
| `data/supporthub.db` | The local database file created when the app first runs. It is ignored by Git. |

The `node_modules` folder contains downloaded libraries. It is created by
`npm install` and should not be committed to Git. The `.next` folder is also
generated by Next.js when the app runs or builds.

## Requirements

Install [Node.js LTS](https://nodejs.org/). npm is included with Node.js.

Check that Node.js and npm are available:

```powershell
node --version
npm --version
```

## Run the app for the first time

Open PowerShell in the repository root, then run:

```powershell
cd .\supporthub
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in a browser. The page
will update automatically when you edit the code.

To stop the app, press `Ctrl+C` in the terminal.

## Useful commands

Run the code checker:

```powershell
npm run lint
```

Create a production version:

```powershell
npm run build
```

Start the production version after building it:

```powershell
npm run start
```

## Making a simple change

To change the ticket form or how new tickets are saved, open:

```text
supporthub/app/NewTicketForm.tsx
```

To change the table columns or how a status looks, open:

```text
supporthub/app/TicketTable.tsx
```

## What will be added later

The app saves new tickets locally. Future versions can add login, a shared
cloud database, ticket updates, and filtering.
